#!/usr/bin/env bash

if [[ $1 -ne '' ]]; then
    dots=""
    for i in `seq 1 $1`; do
        dots="$dots../"
    done
    echo $dots
else
    cat << EOF
Usage: cdup NUMBER

Generate an argument for cd that climbs a directory structure NUMBER of times.
EOF
fi
